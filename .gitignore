node_modules/
dist/
dist-ssr/
*.local

# Generated content index — rebuilt by npm run content:build
content/manifest.json

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Test / build output
coverage/
*.tsbuildinfo

# Local progress exports (may contain personal study history)
*.progress.json

# Ready but unpushable: needs a gh token with the workflow scope (see README).
.github/workflows/deploy.yml
